server:
  port: 8080

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost/smtp_repository
    username: postgres
    password: 4253
  jpa:
    hibernate:
      ddl-auto: update